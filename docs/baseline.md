# 基线测量 · 2026-07-20 22:20

| 阶段 | 耗时(s) | 数量 |
|---|---|---|
| HN fetch | 66.02 | 383 |
| arXiv fetch | 8.26 | 97 |
| filter+score | 0.01 | 112 |
| dedup | 0.00 | 112 |
| **总计** | **74.29** | — |

原始 480 → 命中 112 → 去重后 112(去重仅移除 0 条,说明**量大不是去重不够,是命中门槛太松/来源太多**)。
config.json 的 max_items_per_source=60 但 main.py 目前没有真正生效裁剪。

---

# 代码基线 · 2026-07-22(优化师阶段)

> 说明:本项目是零 pip 依赖的纯 stdlib Python 3.9 项目,没有 Cargo/clippy 工具链。
> 下表用项目实际存在的对应工具测量:`python3 -m unittest discover`(测试)、
> `scripts/lint.sh`(项目自建的零依赖语法门禁,`python3 -m compileall`)、
> `wc -l`(代码行数)、`time python3 -m compileall`(最接近「构建」的真实可执行步骤,
> 无编译型 build 可测)。全部数字为本次真实执行的原样摘录。

## 测试:`python3 -m unittest discover -s tests -v`

```
Ran 32 tests in 0.017s

OK
```
**32 个测试,全绿。**

## 语法门禁:`bash scripts/lint.sh`

```
语法门禁通过
EXIT=0
```
**0 个错误/警告。**(项目未装 ruff/mypy,零依赖是设计选择,见 `scripts/lint.sh` 注释;因此没有 clippy 式的警告计数维度,门禁只保证语法合法可导入。)

## 代码行数:`wc -l aihot/*.py aihot/sources/*.py`

| 文件 | 行数 |
|---|---|
| aihot/__init__.py | 0 |
| aihot/dedup.py | 27 |
| aihot/filter.py | 37 |
| aihot/main.py | 122 |
| aihot/render.py | 160 |
| aihot/sources/__init__.py | 0 |
| aihot/sources/arxiv.py | 58 |
| aihot/sources/hackernews.py | 60 |
| **合计(源码,不含测试)** | **464** |

测试代码(`wc -l tests/*.py`)合计 **465** 行 — 与源码行数(464)几乎 1:1,测试体量未失控。

## 「构建」耗时:`time python3 -m compileall -q aihot tests`(清空 `__pycache__` 后冷测)

```
0.03s user 0.01s system 93% cpu 0.036 total
```
**0.036s。**(无编译型构建步骤可测;这是项目实际拥有的、语义最接近「构建」的真实命令。)

## 结论

- 测试、门禁、字节码编译全部真实通过,当前代码库健康。
- 源码 464 行,体量不大,尚无需要「删」的明显冗余入口;后续每次改动后应重跑本页四项命令并记录 delta,任何一项行数上升都要能说明理由。
- 与 2026-07-20 的抓取耗时基线合并看:系统瓶颈在**网络抓取**(74.29s,HN 占 89%)而非代码本身;若要谈「扛得住更多人用」,优化方向应优先看 HN fetch 的并发/超时策略,而不是代码结构。

---

# 优化实施 delta · 2026-07-22(优化师阶段)

**改动**:合并 `render.py` 里重复两处的"截断到 N 字符 + 超长加省略号"逻辑(`docs/bottlenecks.md` 第 4 条),
提成 `_truncate(text, limit)` 一个函数,两处调用点(Markdown 160 字符、HTML 220 字符)改为调用它。
纯等价重构,不改变任何输出内容(HTML 分支的 `html.escape` 顺序在数学上等价:省略号"…"本身不含
HTML 特殊字符,escape 顺序不影响结果)。

第 2/3 条(`id`/`url`/`time`/`score` 冗余字段)**本阶段未删**:`docs/SPEC.md:44-45` 把这些字段
明文列为 HN/arXiv 数据源契约的一部分,删除属于契约变更,不是等价重构,不在本阶段"只改不加"的
授权范围内,如实标注为「留待下一棒决策」而非直接动手。第 1 条(HN 并发)和第 5 条(arXiv 顺序抓取)
同理未动:调整并发度需要真实压测验证是否触发 HN API 限流(`docs/bottlenecks.md` 已标注「未验证」),
本阶段没有做真实压测,不能在没有验证的情况下改变网络行为。

## 重测(与本页基线完全相同的命令)

**测试**:`python3 -m unittest discover -s tests -v`
```
Ran 32 tests in 0.013s

OK
```
32 个测试,全绿,与基线一致(基线 0.017s → 0.013s,量级相同,属正常抖动)。

**语法门禁**:`bash scripts/lint.sh`
```
语法门禁通过
EXIT=0
```
与基线一致。

**代码行数**:`wc -l aihot/*.py aihot/sources/*.py`

| 文件 | 基线行数 | 改动后 | delta |
|---|---|---|---|
| aihot/render.py | 160 | 162 | **+2** |
| 其余 6 个文件 | 304 | 304 | 0 |
| **合计** | **464** | **466** | **+2** |

**如实说明 delta 为正**:两处调用点各减少 1 行(共 -2),但新增的 `_truncate` 函数本身占 2 行
代码 + 1 行分隔空行(共 +4),净增 +2。去重复没有换来净删代码——两个调用点数量太少(仅 2 处),
提取成本(函数签名 + 空行)超过了消除重复省下的行数。这是本阶段"删减优先"戒律下的真实结果,
不因为"应该是负数"就少报或调整口径。逻辑重复(两份阈值不同的截断代码)已消除,是本次改动的
真实收益,但不等于行数下降。

**构建耗时**:`time python3 -m compileall -q aihot tests`(清空 `__pycache__` 后冷测)
```
0.03s user 0.01s system 72% cpu 0.051 total
```
与基线 0.036s 同量级(冷启动 Python 解释器开销主导,±0.015s 属正常噪声,非真实回归)。

## 未动的项(如实记录,不是遗漏)

- **第 1 条(HN 并发)**:唯一有真实性能数据支撑的瓶颈,但调整 `max_workers` 需要真实压测验证
  HN API 限流风险,本阶段未做压测,不能凭猜测改网络并发参数。留给下一棒,若要动手需先做
  真实压测并记录结果。
- **第 2/3 条(冗余字段)**:`docs/SPEC.md` 契约字段,删除是契约变更不是等价重构,留给下一棒
  做出"是否修改 SPEC"的决策后再删。
- **第 5 条(arXiv 顺序抓取)**:影响小(8.26s/11%),且给它加并发是新增并发结构,优先级低于
  第 1 条,本阶段不动。
