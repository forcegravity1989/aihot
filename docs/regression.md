# 回归验证 · 2026-07-20 22:24(对比 docs/baseline.md)

| 阶段 | 优化前 | 优化后 | delta |
|---|---|---|---|
| HN fetch 耗时 | 66.02s | 33.50s | -32.52s |
| HN 条目数 | 383 | 199 | -184 |
| 总耗时 | 74.29s | 46.22s | -28.07s |
| 最终输出条目 | 111 | 30 | -81 |

结论:耗时下降;输出条目从 111 降到 30——达到「日报」应有的可读体量。

---

# 回归验证 · 2026-07-22(优化师阶段末,对比代码基线)

> 用与 `docs/baseline.md`「代码基线 · 2026-07-22(优化师阶段)」一节完全相同的四条命令重新
> 测量一次,作为本阶段结束前的最终回归核验。命令、环境、清空 `__pycache__` 的步骤均与基线一致。

## 1. 测试:`python3 -m unittest discover -s tests -v`

```
Ran 32 tests in 0.012s

OK
```

| | 基线 | 本次回归 | delta |
|---|---|---|---|
| 测试数 | 32 | 32 | 0 |
| 耗时 | 0.017s | 0.012s | -0.005s(同量级抖动) |
| 结果 | OK | OK | 无回归 |

## 2. 语法门禁:`bash scripts/lint.sh`

```
语法门禁通过
EXIT=0
```

与基线一致,EXIT=0,无回归。

## 3. 代码行数:`wc -l aihot/*.py aihot/sources/*.py`

```
      0 aihot/__init__.py
     27 aihot/dedup.py
     37 aihot/filter.py
    122 aihot/main.py
    162 aihot/render.py
      0 aihot/sources/__init__.py
     58 aihot/sources/arxiv.py
     60 aihot/sources/hackernews.py
    466 total
```

| 文件 | 基线 | 本次回归 | delta |
|---|---|---|---|
| aihot/render.py | 160 | 162 | +2 |
| 其余 6 个文件 | 304 | 304 | 0 |
| **合计** | **464** | **466** | **+2** |

delta 与本阶段"优化实施 delta"小节记录的 +2 完全一致,来源已在 `docs/baseline.md` 中说明:
`_truncate` 提取消除了两处重复的截断逻辑,但函数签名+空行的固定开销(+4)大于两处调用点省下的
行数(-2),净增 +2。这是唯一的行数变化项,无意外新增。

## 4. 「构建」耗时:`time python3 -m compileall -q aihot tests`(清空 `__pycache__` 后冷测)

```
0.03s user 0.01s system 93% cpu 0.039 total
```

| | 基线 | 本次回归 | delta |
|---|---|---|---|
| 总耗时 | 0.036s | 0.039s | +0.003s |

冷启动 Python 解释器开销主导,±0.015s 级别波动在本阶段两次重测(0.051s、0.039s)中都出现过,
判定为噪声,非真实回归。

## 结论

- 四项指标(测试数、门禁、行数、构建耗时)与基线逐一核对,**无回归**:测试仍为 32/32 全绿,
  门禁仍为 EXIT=0,构建耗时在噪声范围内。
- 唯一的净变化是代码行数 +2,已在 `docs/baseline.md`「优化实施 delta」一节如实说明成因
  (提取重复逻辑的函数开销 > 省下的重复行数),不是回归,是本阶段"删减优先"戒律下如实报告的
  真实结果——逻辑重复被消除,但行数未下降。
- 本阶段唯一改动(`render.py` 的 `_truncate` 重构)是等价重构,不改变任何输出内容,回归验证
  确认无副作用。
