# aihot 日报

**类型**:Web 应用 · AI 热点日报

AI 热点圈信息过多——按用户自己的关注面(关键词配置),每天从多个真实来源(Hacker News、arXiv)聚合、去重、过滤,生成一份可读的每日 AI 热点摘要。

## 定位与机会

- **对标**:Feedly/RSS 阅读器(需要自己订阅源,无 AI 相关性过滤);各类「AI 日报」公众号/newsletter(人工编辑,非自动化,更新看编辑心情)
- **机会**:三个月内:每天真实生成一份不需要手动修正的 AI 热点摘要,覆盖至少两类真实来源(社区热度 + 学术前沿),命中用户自定义的关注面。

## 北极星(三个月成功标准)

每周「读完且有收获」天数

> 定义:过去 7 个自然日中,构建者本人在 `docs/usage-log.md` 逐日记录为「今天打开了当日日报 + 读完了 + 其中至少 1 条是我事前不知道、且确实命中我关注面的真实信息」的天数(0-7);当天记录缺失计为「否」,不计中性。
>
> 正本在 `.bw/metrics.toml`,推导过程(6 判据打分、被拒的坏候选、反指标、0-40 质量门)在 `docs/metrics-rationale.md`。

**创建期的旧北极星(已废,留档不删)**:「连续 7 天,每天都真实生成一份包含 ≥5 条命中关注面的摘要」——按 `digests/YYYY-MM-DD.md` 是否存在逐日核验。它被自己的判据挡下:整条量的是**文件有没有被生产出来**(供给段),挂一条永不失败的定时任务即可永久满分,而「有没有人读、有没有因此变好」一个字没量到。

---

> 本章程由 Builders' Workbench 在创建流程中逐步写就,每次更新留一次提交。
